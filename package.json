{
  "name": "social-jarvis-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Internal Social Jarvis Automation Hub dashboard.",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "start:env": "node --env-file=.env server.mjs",
    "stack:start": "node --env-file=.env scripts/start-local-stack.mjs",
    "stack:check": "node --env-file=.env scripts/check-integrations.mjs",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "health": "node scripts/healthcheck.mjs",
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
