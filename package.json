{
  "name": "social-jarvis-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Internal Social Jarvis Automation Hub dashboard.",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "start:env": "node --env-file=.env server.mjs",
    "build": "node scripts/check.mjs",
    "check": "node scripts/check.mjs",
    "health": "node scripts/healthcheck.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.101.0"
  }
}
